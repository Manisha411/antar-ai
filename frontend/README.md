# Frontend – Journal Companion

React (Vite) + Tailwind. Login, Today (context-aware prompt + write + save), History (list entries).

## Run

```bash
npm install
npm run dev
```

Port: **3000**. Set env (or `.env`; see `.env.example`):

- `VITE_AUTH_URL`, `VITE_JOURNAL_URL`, `VITE_PROMPT_URL`, `VITE_INSIGHTS_URL`, `VITE_SUMMARY_URL`
