# Copy this file to .env and use the same values when starting all services.
# In each terminal, from repo root run:  source .env
# Then start the service for that terminal (see RUNBOOK.md).

# You create this secret (no service generates it). Use the same value for Auth, Journal, Insights, Summary.
# One ready-to-use value (32+ chars for HS256). Change in production.
JWT_SECRET=journal-companion-dev-secret-32chars-minimum!!

# Optional: override defaults
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432
# POSTGRES_DB=journal_db
# POSTGRES_USER=journal
# POSTGRES_PASSWORD=journal_secret
# KAFKA_BOOTSTRAP_SERVERS=localhost:9092
# ANALYTICS_DB_URL=postgresql://analytics:analytics_secret@localhost:5433/analytics_db
# JOURNAL_SERVICE_URL=http://localhost:8080

# Service ports (optional)
# PORT=3001   # Auth
# SERVER_PORT=8080   # Journal
# PORT=8000   # Prompt
# PORT=8001   # Insights
# PORT=8002   # Summary
